name: Docker Compose

on:
    workflow_dispatch:

jobs:
    deploy:
        env: 
            DOCKERHUB_USER : ${{ vars.DOCKERHUB_USER }}
        
        runs-on: self-hosted

        steps:
            - name: checkout code
              uses: actions/checkout@v4

            - name: deploy with docker compose
              run: docker build